function addToCart(product) {
    alert(product + " has been added to your cart!");
    // Here you could add functionality to actually add the product to a shopping cart
    // This could involve storing the cart data in local storage or sending it to a server
}
